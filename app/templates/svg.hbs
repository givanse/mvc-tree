
<!-- Debug grid -->
{{#if svgenv.showGrid}}
  {{#each line in gridLines}}
    <path d="{{unbound line}}" fill="none" stroke="#909090"></path>
  {{/each}}
{{/if}}

<!-- Years lines -->
{{#each line in yearLines}}
  <text x="{{unbound line.x}}" y="{{unbound line.y}}" class="year_line_txt">
    <tspan dy="4.233003616333008">
      {{line.year}}
    </tspan>
  </text>
  <path d="{{unbound line.path}}" stroke="#000000" fill="none"></path>
{{/each}}

<!-- Column Headers -->
{{#each elem in model.headers}}
  {{view 'svg-g' templateName='node-header'
                 treeConfig=svgenv
                 elem=elem}}
{{/each}}

<!-- Design Patterns -->
{{#each elem in model.dpatterns}}
  {{view 'svg-g-click' templateName='node-dpattern'
                 treeConfig=svgenv
                 elem=elem}}
{{/each}}

<!-- Technologies -->
{{#each elem in model.technologies}}
  {{#each className in elem.classNames}}
    {{view 'svg-g-click' templateName='node-technology' 
                   treeConfig=svgenv
                   elem=elem
                   classNameTech=className}}
  {{/each}}
{{/each}}

<!-- links -->

