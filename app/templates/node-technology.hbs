
<ellipse class="node_tech" cx="{{unbound view.cx}}"
                           cy="{{unbound view.cy}}" 
                           rx="{{unbound view.rx}}" 
                           ry="{{unbound view.ry}}"></ellipse>

<text class="node_txt" x="{{unbound view.cx}}" y="{{unbound view.y}}">
  <tspan dy="32">
    {{elem.name}}
  </tspan>
  <tspan x="{{unbound view.cx}}" dy="16.8">
    {{elem.year}}
  </tspan>
</text>
